# Handoff — B1 (`ferrite-ipi` vocabulary migration) — first of the new post-A13 charter sequence (B1–B4)

**Session note:** worktree started on an unrelated tree. Recreated
`rebuild/b01-ipi-vocabulary-migration` from local `main` (`7979a04`, the A13
merge) before any work — same hazard every agent since A4 has hit.

**Landed (`e7ddd91`→`29b7840`→`75321e4`→`cd5d090`):** `ferrite-ipi`'s
`Cargo.toml` no longer depends on `ferrite-agent` (T-221, grep-confirmed).
`dry_run` rebuilt on `ferrite_engine::BrowserEngine` directly (`DryRunEngine`,
new; `RecordingExecutor`/`executor.rs` deleted). `ToolEvent` carries a real
`ferrite_core::Primitive` (T-216's bridge deleted at its source). `tool_decision`'s
old `LlmMayUsePredictor`/`ToolFingerprint`/old `rule_based_must_use` deleted;
`generate_fingerprint` now takes `&dyn ferrite_model::ModelProvider`
explicitly and calls A4's real predictor. Added purely-additive
`ferrite_agent::engine_bridge::EngineToolExecutor` so existing `AgentRuntime`
implementors can drive the new vocabulary. `ferrite-eval`/`ferrite-ui` given
minimal, verified-behavior-preserving compile fixes (full list in
`docs/PROGRESS.md`'s B1 entry). **Verified, not just compiling:** `cargo run
-p ferrite-eval --example eval` still reports 29 cases/96 executions with
every headline metric identical to `docs/EVALUATION.md`. 157/15/125/17/154
tests pass across ipi/agent/eval/ui/model; `cargo build --workspace`, `fmt
--all --check`, `clippy --workspace --all-targets -D warnings`, `cargo deny
check` all clean.

**B2 (`ferrite-eval` migration off `BrowserTool`/`AgentRuntime`) — your exact
next action:**
1. `ferrite-eval` still depends on `ferrite-agent` throughout
   `worst_case_agent.rs`, `corpus.rs` (`KNOWN_TOOL_IDS`, `by_tool` JSON
   keys), `harness.rs` (my new `AgentRuntimeDriver` bridges, does not
   migrate). Deciding whether/how far to move onto `ferrite_engine`/
   `Primitive` directly is yours — the bridge buys a working system today
   without forcing that decision on B1.
2. `harness::run_one` hardcodes `ferrite_model::MockProvider::new()` for the
   fingerprint call. Wiring a real, configured `ModelProvider` in and
   re-running `just eval` for fresh numbers is yours.
3. `content_key_for_call` (`ferrite-ipi/src/dry_run/engine.rs`, T-230)
   doesn't cover `BrowserEngine`-only actions (`scroll`, `wait_for`,
   `tab.*`, `cookie.read`, `storage.read`, `query`) — extend
   `DryRunContent`'s scripting surface if your corpus migration authors
   cases against them.
4. Update `docs/EVALUATION.md` once your migration + a real provider land.

**B3 (`ferrite-ui`/`ferrite-shell` live wiring, T-224) — your exact next
action:** `ferrite-ui`'s `AgentTaskSubmitted` handler hardcodes
`MockProvider::new()` (T-229) — your charter was already constructing a real
`ModelProvider` here, so closing T-224 closes T-229 as a side effect. The
dry-run half is already on `DryRunEngine`/`DryRunDriver` (via
`DryRunAgentDriver`, mirroring `ferrite-eval`'s `AgentRuntimeDriver`); your
"live execution" sibling concept is giving `HeadlessServoSession` a real
`BrowserEngine` face (T-220's live-app side) — nothing in B1 blocks that.

**Risk assessment:** T-221's fix is real and verified, not just claimed. The
one risk handed forward: `ferrite-eval`/`ferrite-ui`'s own migration off
`BrowserTool` is now B2/B3's to do against a *stable* vocabulary rather than
a moving one — their internal logic/test assertions were not touched, only
the compile surface.
