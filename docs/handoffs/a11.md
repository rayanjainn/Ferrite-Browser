# Handoff — A11 (Dataset + corpus)

**Session note:** same hazard A4–A10 hit — worktree HEAD started on the
stale pre-rebuild tree. Recreated `rebuild/a11-dataset-corpus` from local
`main` (`53c8190`, carries A0–A10) before any work; nothing lost (working
tree was clean).

**Scope-mismatch note confirmed:** the directive's file list names
`crates/ferrite-eval/src/dataset/`, `corpus/`, but the real types and loader
live in the flat files `crates/ferrite-ipi/src/dataset.rs` and
`crates/ferrite-eval/src/corpus.rs`, as the launch brief anticipated. Worked
in both; no new `dataset/`/`corpus/` directories created.

**Landed (`6e6926a`, `2bc1925`, `d1531c0`):**
- **T-006/D6 closed:** `CarrierVector` is now `WebContent(WebContentVector)`
  / `ToolOutput(ToolOutputVector)` — the mismatched pairing cannot be
  constructed, in Rust or JSON. Old runtime check deleted, not superseded.
- **T-009/D9 closed (implemented for real):** `ferrite_eval::agentdojo`
  adapts 3 real, cited AgentDojo Slack-suite injection tasks onto Ferrite's
  browser tool surface via a documented tool-substitution table. 3 cases
  seeded in `tests/agentdojo_corpus/`.
- **T-208 closed:** all 10 `tests/pilot_corpus/*.json` reshaped and
  re-validated; still single-authored.
- **T-201 closed:** `CORPUS_AUTHORING_GUIDE.md` fixed in place (real,
  pre-existing drift in its `OriginScope`/`carrier` sections found and
  corrected) + AgentDojo section added.
- **T-203 closed:** schema audit against §13.2 — 2 real gaps filed (T-225
  FSR is case-level only; T-226 no token-usage field for ΔT).
- **16 new corpus cases** in `tests/corpus/` (6 Tier1, 4 Tier2, 6 benign).

**Corpus total: 29 cases** (Tier1 17, Tier2 9, Tier3AgentDojo 3; Attack 22 /
Benign 7) — **far short of §13.3's ~360 target.** 0% double-authored (single
agent session, no independent second author available); κ not computed,
stated honestly rather than fabricated. Full remainder: **T-227**.

**Tests:** 76 in `ferrite-eval` (61 lib + 3+4+3+5 across 4 integration
binaries), 11 in `ferrite-ipi::dataset`, all green. `cargo fmt`/`clippy
--all-targets -D warnings` clean on both crates.

**A12's exact next action** (`crates/ferrite-eval/src/harness/`,
`adjudication/`, `metrics/`, `docs/EVALUATION.md`):
1. The 4-mode matrix already exists in `harness.rs::run_label`/`mode_behavior`
   — enforce/extend it in code per your own charter, not from scratch.
2. Fix T-215 first (one-line: `harness.rs::run_one` still calls
   `set_detect_enabled` instead of `set_defense_mode` — D3/T-003 isn't fully
   live without it).
3. Build the metrics module against **n=29**, not n≈360 — your power/CI
   claims must be scoped to the real corpus (see T-227's exact composition).
   T-225/T-226 mean FSR and ΔT need either a schema addition (yours to make,
   `dataset.rs` is no longer under A11's exclusive charter) or an explicit
   documented limitation in `EVALUATION.md`.
4. D10/T-010 (consent-policy sweep) and D4/T-004 (outcome lattice) are still
   open — your charter's own mandatory items.
