# Handoff — A1 (Foundation)

**Landed:** `[workspace.dependencies]` consolidation + trimmed tokio/chrono
features + build profiles; closed T-207 (workspace-wide fmt+clippy
`--all-targets` clean); `deny.toml` (advisories/bans/licenses/sources all
`ok`) — which surfaced and fixed real security advisories in direct deps
(ammonia XSS, rand unsoundness, rustls-webpki cert validation); `license`
+ `publish = false` on all 7 crates; `rust-toolchain.toml`; `.cargo/config.toml`
(target-dir deliberately left to the justfile, documented why); `justfile`
(every recipe run and verified except `bloat`/`clean-cache`, which need
tools not installed this session); `.github/workflows/ci.yml` reworked
(ubuntu+macos+windows, Servo weekly not per-push, all lint gates wired);
`docs/BUILD_BUDGET.md` with real numbers. Full detail + commit SHAs in
`docs/PROGRESS.md`'s 2026-09-18 A1 entry.

**Needs owner confirmation before going further, filed as T-209/T-210:**
(1) the `MIT OR Apache-2.0` license added to every crate — inherited from
the archived `.rules` file's stated intent, not freshly confirmed this
session; no LICENSE-MIT/LICENSE-APACHE files exist yet either. (2) CI's
Servo-release cadence changed from every push to weekly+manual — a real,
visible behavior change to a pipeline the pre-rebuild team built
deliberately. Nothing pushed to a remote yet (17 local commits on `main`),
so no external effect yet, but don't push without the owner having seen
this.

**Not done, in scope but skipped:** `cargo-bloat`/`cargo-sweep` not
installed (recipes exist, gracefully say so if the tool's missing);
`.github/workflows/ci.yml` not remotely verified (this sandbox can't run
GitHub Actions — YAML validated locally, every command it runs was
executed on this machine directly instead).

**Discovered, not A1 scope, filed in `docs/TO-DO.md`:** none new beyond
T-207 (now closed) and T-209/T-210 above.

**Verified, not assumed:** `just check`, `just test` (95+ tests),
`cargo deny check` (exit 0), `cargo machete` (clean), a from-wiped-
target-dir timing run (3m32s), and `cargo build --release -p
ferrite-shell` (no Servo) all actually run on this machine this session.

**Exact next action:** A2 (Core contracts) — `crates/ferrite-core`: capability/
primitive taxonomy, newtype IDs, per-capability `OriginScope` as a
first-class type (this is what T-001/T-002/A7 ultimately need), `Clock`
trait, error enums, config loading. Read `docs/REBUILD_DIRECTIVE.md` §6/A2
+ §8 + this handoff + `docs/DECISIONS.md` ADR-001/ADR-004 only.
