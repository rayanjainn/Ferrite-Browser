# Handoff — B2 (`ferrite-eval` migration off `BrowserTool`)

**Session note:** the subagent that did the bulk of this charter was cut
off mid-edit by a platform rate limit (writing `docs/EVALUATION.md` §2.6).
No commits had landed. The coordinating session independently verified
everything in the working tree (did not trust the interrupted session's
self-report), finished the doc, ran a fresh live-provider verification
round itself, and completed the remaining process steps — mirroring A3's
precedent for a mid-session interruption.

**Landed (`85261b3`):** `WorstCaseAgent` now drives `ferrite_engine::
BrowserEngine` directly (not bridged through `AgentRuntime`) — `ferrite-eval`
no longer depends on `ferrite_agent::{BrowserTool, AgentRuntime,
ToolExecutor}` for its own logic at all (grep-confirmed). `corpus.rs`'s
`KNOWN_TOOL_IDS` is derived from `ferrite_core::Primitive::ALL` directly.
`dry_run::engine::content_key_for_call` (B1) updated to the same
`Primitive::as_str()` vocabulary. T-221 now closed on both `ferrite-ipi`
and `ferrite-eval`'s sides.

**Real live-provider `just eval` run — attempted, succeeded, verified
twice, byte-identical both times** (`FERRITE_MODEL_SMALL=
FERRITE_MODEL_MAIN=gemma4:31b`, key from the OS keyring). Only
fingerprint-dependent metrics moved (`LoopOnly` ASR 1/19→3/19, `On`'s
benign FGR 3/7→1/7), both in the expected direction; everything
sanitizer/audit-only is byte-identical to the rules-only default run.
Full numbers: `docs/EVALUATION.md` §2.6 (new). `docs/EVALUATION.md`'s §4
model-backend row and §7 checklist item (both previously describing code
B1 already deleted) were corrected to reflect what actually runs now, not
just extended with new content.

**Verified independently by the coordinator, not carried forward:**
`cargo test -p ferrite-eval` 126 (up from 125), `-p ferrite-ipi` 157
(unchanged — the vocabulary edit is a rename). `cargo build --workspace`,
`cargo fmt --all --check`, `cargo clippy --workspace --all-targets -D
warnings` all clean. `cargo test --workspace` — 30 `test result: ok`
blocks, 0 failures. `cargo deny check` clean. The default (no env vars)
`just eval` run reproduces the pre-B2 committed numbers exactly — 29
cases, 96 executions.

**T-230 not closed** (genuinely nothing to extend it against — no corpus
case exercises `scroll`/`wait`/`tab.*`/`cookie.read`/`storage.read` yet),
its row updated to reflect the vocabulary unification without overclaiming
the coverage gap is fixed.

**Exact next action for B3 (`ferrite-ui`/`ferrite-shell` live wiring,
T-224/T-229/T-220):**
1. Construct a real `ferrite_model::ModelProvider` at `ferrite-ui`
   startup — mirror `ferrite-eval::harness::try_real_provider()` (now a
   working, tested reference implementation of exactly this: `ModelConfig::
   from_env()`, Ollama first, keyring-backed). Pass it into
   `tool_decision::ToolDecisionEngine::generate_fingerprint` (B1's new
   signature) instead of the `MockProvider::new()` `ferrite-ui` currently
   hardcodes (T-229) — this single change closes T-229 as a side effect of
   closing T-224.
2. `ferrite-ui`'s live tool-execution path (`BrowserToolExecutor`/
   `FilteredToolExecutor`, driven by `ferrite_agent::{GeminiAgent,
   BrowserTool, AgentRuntime, ToolExecutor}`) is the one remaining caller
   of the old vocabulary anywhere in the workspace. Giving the live
   `HeadlessServoSession` (already driven by a real winit loop via Iced) a
   `BrowserEngine` face is the natural fix for T-220 as a side effect —
   `ferrite_engine_servo::ServoEngine`'s DOM-via-JS-injection logic is
   already written and can very likely be reused/adapted rather than
   rebuilt, once it's driven by the session the live app already owns
   instead of trying to bootstrap its own.
3. `WorstCaseAgent` (this charter) is a genuine, working reference for
   "how to drive `BrowserEngine` directly from calling code" — read
   `crates/ferrite-eval/src/worst_case_agent.rs` before designing
   `ferrite-ui`'s equivalent live-execution path.
4. Once B3 lands, `ferrite_agent::{BrowserTool, AgentRuntime, GeminiAgent,
   ToolExecutor}` should have zero remaining callers anywhere in the
   workspace — confirm with a full-workspace grep before deleting them.
