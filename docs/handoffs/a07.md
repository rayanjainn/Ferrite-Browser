# Handoff — A7 (Comparator + origin model)

**Session note:** same hazard A4/A5/A6 hit — worktree started on a stale,
unrelated tree (`65b6d67`, "Initial test case designing..."/unrelated
history). Recreated `rebuild/a07-comparator-origin-model` from local
`main` (`69f4268`, carries A0–A6) before doing any work.

**Landed:** `crates/ferrite-ipi/src/comparator/{mod,expected,diff,consent,
legacy}.rs` replace the flat `comparator.rs`. New contract: `compare(
expected: &ExpectedFingerprint, actual: &DryRunRecord) -> FingerprintDiff`.
`ExpectedFingerprint` wraps `ferrite_core::ExpectedCapabilitySet` (A2) —
per-capability `OriginScope`, admission-rank attribution via
`Specificity`'s `Ord` (T-001/T-002 closed). T-206's double-flag bug fixed
by construction (strict partition, no `Both` bucket — reasoning in the
module docs). T-211 ratified as by-design (`Origin::parse`'s http(s)-only
acceptance is the correct signal, not a gap). T-216 resolved with a
one-entry mapping local to `comparator::resolve_primitive`
(`"download.file" -> Primitive::Download`), not a `ferrite-agent` change.

**The design gap (neither `fingerprint::Fingerprint` nor legacy
`ToolFingerprint` carries a per-capability scope):** resolved via
`ExpectedFingerprint::from_fingerprint`/`from_legacy_tool_fingerprint`,
which default every capability to `exact(context_url)` when a context URL
exists, `task_open` with a stated rationale otherwise — never an
unconditional `task_open` (regression test:
`expected::tests::from_fingerprint_with_a_context_url_still_flags_a_different_origin`).
`ExpectedFingerprint::from_capabilities` is the direct constructor for a
caller with real per-capability scopes. Filed **T-217** for the actual
authoring mechanism (wiring `dataset::CaseDefinition` or a UI into it) —
open, owned by A11/A12/whoever.

**Cross-crate compile fixes** (minimal, commit `40221de`):
`ferrite-eval/src/harness.rs` (context_url extraction + `compare()` call
site), `adjudication.rs` (test-only `OriginScope` migration),
`corpus.rs`/`tests/pilot_w6.rs`/`tests/pilot_corpus/*.json` (10 files,
`expected_origins` wire-shape migration), `ferrite-ui/src/lib.rs` (its
`compare()` call site now derives a real exact-scope from
`agent_task.context_url` instead of a hardcoded `task_open()`, closing
that file's stale `TODO(Task 19)`). Both gained a direct `ferrite-core`
dependency (downward edge, already in root `Cargo.toml`; machete clean).

**Verified:** `cargo test -p ferrite-ipi` (166 passed + 2 doctests, 37 new
in `comparator/`), `fmt`/`clippy --all-targets -D warnings`/`machete`
clean. `cargo build|test --workspace` green (no `FAILED`/`error[` in the
output). `just check`/`just test` green. `cargo deny check` clean except
the pre-existing Servo-git-source warning (same as A3–A6).

**Commits:** `6cb4c70` (comparator rebuild), `40221de` (cross-crate fixes).

**Exact next action for A8:** `crates/ferrite-audit`, per §6/A8 (T-005/
T-108). Today's hash covers `sequence, timestamp, kind, principal_id,
prev_hash` — not `capability`/`url`, so `exec_id`/`case_id` on an
`EvalExecutionRecorded` entry can be rewritten with `verify_chain()`
still passing. Fix: hash the full canonical, versioned entry payload,
domain-separated (`b"ferrite-audit-v1"` prefix), `schema_version` byte.
**What this session hands you:** `comparator::FingerprintDiff` now
carries `attributions: Vec<Attribution>` (tool, origin, admitting
`Capability`, winning `Specificity`) for cite-able justified actions;
`extra_primitives`/`out_of_scope_origins` (unchanged shape) are what a
consent-gated entry should cite for a flagged one.
