# Handoff — A9 (Engine + agent action surface)

**Session note:** same hazard A4–A8 hit — stale, unrelated worktree
(`65b6d67`). Recreated `rebuild/a09-engine-agent` from local `main`
(`b4f4c8c`, A0–A8) before any work.

**Landed:** `crates/ferrite-engine` (new, always built) — `BrowserEngine`
trait (24-method action surface, every method returns `(T, Origin)`),
`MockEngine` (scripted, origin-scoped, 24 conformance tests, green).
`crates/ferrite-engine-servo` (new, feature `engine-servo`) — `ServoEngine`
wraps `ferrite_servo::session::HeadlessServoSession`; DOM ops via injected
`JSON.stringify` scripts + a documented `execute_js` Debug-unwrap;
`download`/`clipboard_*` honest `Unsupported`; `cookies_read`/
`storage_read` refuse a non-current-page origin instead of navigating
silently. `crates/ferrite-agent/src/browser_loop.rs` (new, additive) —
plan→act→observe loop over `&dyn BrowserEngine` + `&dyn
ferrite_model::ModelProvider`, step/wall-clock/repeat budgets, 8 tests.
`BrowserTool`/`AgentRuntime`/`ToolExecutor`/`GeminiAgent` kept as-is —
still load-bearing for `ferrite-ipi::dry_run`/`ferrite-eval`/`ferrite-ui`.

**Real finding, fixed:** `BrowserEngine` originally required `Send`. Real
`HeadlessServoSession` holds `Rc<...>` throughout (thread-affine Servo
engine), so building with `--features engine-servo` failed with 24
"cannot be sent between threads" errors. Fixed by dropping `Send` from
the trait (reasoning in its doc comment).

**Exit gate:**
1. MockEngine conformance: **done**, 24/24, part of `just test`.
2. `just build-servo`: **succeeded**, 15m31s, 6.4GB, exit 0 —
   `docs/BUILD_BUDGET.md`'s A9 entry.
3. `ServoEngine` conformance: **attempted, partial.** `ServoEngine::new`
   succeeds against the real build (also fixed: Servo's `opts` singleton
   panics if constructed on a 2nd OS thread in one process — Rust spawns
   one per `#[test]` fn — fixed by consolidating to one sequential test).
   But `navigate()` to a loopback fixture never completes — no TCP
   connection attempt observed at all. Not fixed: likely
   `HeadlessServoSession` needs a real winit loop (as `ferrite-shell`
   provides) to make networking progress, not bare `spin_event_loop()`.
   Filed **T-220**. Detail: `ferrite-engine-servo/tests/
   servo_conformance.rs` module docs, `docs/PROGRESS.md`'s A9 entry.

**Also filed T-221:** `ferrite-ipi` still depends on `ferrite-agent`
(backwards direction) — confirmed present, not fixed here, flagged for A13.

**Verified:** `-p ferrite-engine` 24/24, `-p ferrite-engine-servo` 5/5
(default features), `-p ferrite-agent` 11/11. `just check`/`just test`
green, isolated `CARGO_TARGET_DIR`. `cargo clippy --features
engine-servo` against the real Servo tree also run — result in
`docs/PROGRESS.md`'s A9 entry.

**Exact next action for A10 (`crates/ferrite-ui`):** consume
`ferrite_engine::BrowserEngine` (`MockEngine` for UI tests),
`ferrite_agent::browser_loop::{AgentAction, AgentLoopResult,
LoopStopReason}` for the consent panel — `AgentAction` is already
serde-tagged/human-legible per variant, each executed action's `Origin`
is available, the admitting-capability field waits on the IPI defense
loop being wired in later (not this charter, not yet A10's). Read
`ferrite-engine`'s and `browser_loop`'s module docs in full first — both
explain real scope boundaries (opaque origins, `js_execute`'s
non-safety, why `BrowserTool` is untouched) A10 should not re-litigate.
