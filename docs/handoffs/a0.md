# Handoff — A0 (Archivist)

**Landed:** repo flattened `Browser/*` → repo root (`e474403`, proven
content-identical via `cargo metadata` before/after); deleted
`ferrite-capability-broker`/`ferrite-policy`/`ferrite-sandbox`/
`extensions/hello-ext`/`.rules`/`commands.md` (`a45b2ad`); merged
`.gitignore`s (`d670dad`); archived `EVALUATION_PLAN.md`/
`FINALIZED_DECISIONS.md`/`PROJECT_REFERENCE.md`/`paper/`/`Resources/*.pdf`/
old `PROGRESS.md`+`TO-DO.md` to `docs/archive/` with `HISTORICAL` banners,
extracted ADR-000..008 into `docs/DECISIONS.md`, wrote `docs/AUDIT.md`
(`9e3f097`+`ffe3c63`, second commit fixes a failed-add in the first — see
`docs/PROGRESS.md`); rewrote `CLAUDE.md`/`README.md` to the zero-status
contract (`01c3f54`); fixed `.devcontainer/{Dockerfile,devcontainer.json}`
for the deleted crates and flatten (`6ccfdad`); installed
`scripts/hooks/{commit-msg,pre-commit,install.sh}`, wrote
`scripts/check_purge.sh` + `scripts/check_no_archive_links.sh`, saved
`docs/REBUILD_DIRECTIVE.md`, fixed `.github/workflows/ci.yml` (broken by
the flatten) (`bf29736`); regenerated `docs/TO-DO.md` as a T-### ledger
(`dc7876e`).

**Verified, not just claimed:** branch check before the move (only `main` +
a 68-day-stale Dependabot branch); `cargo metadata --no-deps` before/after
the flatten, identical package set and signatures; `scripts/check_purge.sh`
and `scripts/check_no_archive_links.sh` both run clean at HEAD; every
`COPY` path and stub-loop crate name in the rewritten Dockerfile checked
against the real tree by hand (no Docker daemon available in this sandbox
to do a real image build — flagged honestly in `docs/PROGRESS.md` rather
than claimed).

**Open, filed in `docs/TO-DO.md`:** T-001–T-014 (defects), T-101–T-113
(charters, none started), T-201–T-208 (corpus-authoring doc, per-case
inspector, schema-completeness audit, the Phase-6 freeze-and-run explicit
*hold*, dropped Extism sandbox, comparator double-flag de-dup, a
newly-found `cargo fmt` gap in 4 crates, and the pilot-corpus
not-grandfathered note).

**Not done, and why:** `docs/ARCHITECTURE.md` doesn't exist — out of A0's
charter (extraction into `DECISIONS.md` was A0's job; the target design doc
is A1/A2's). One trivial comment fix in `ferrite-servo/src/shell.rs` was
reverted rather than committed — it triggered the new pre-commit hook's
crate-scoped `fmt`/`clippy` gate against pre-existing, unrelated debt in a
crate that's reference material for A9, not A0 scope (filed as T-207
instead of forced through).

**Exact next action:** run **A1** per `docs/REBUILD_DIRECTIVE.md` §6 —
`[workspace.dependencies]`, build profiles, `justfile`, `deny.toml`, CI
matrix rework, and wire `scripts/check_purge.sh`/`check_no_archive_links.sh`
into CI (currently run-by-hand only). Read this handoff + §1/§6/§7/§12 +
`docs/TO-DO.md`'s T-101/T-013/T-207 rows only — do not re-read the archive.
