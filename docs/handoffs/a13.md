# Handoff — A13 (Reconciliation & release) — last of the original A0–A13 charter sequence

**Session note:** same hazard A4–A12 hit — worktree branch started on a
wholly unrelated tree (`65b6d67`/`46b2177`/`86d7d3c`, no `docs/`/`crates/`
shape). Recreated `rebuild/a13-reconciliation-release` from local `main`
(`5d4738e`, carries A0–A12) before any work.

**Landed:** doc-claim audit (13 SHAs spot-checked against real `git log`,
all matched); fixed one real stale status in `docs/TO-DO.md` (T-215 said
`in-progress`, actually closed by `31d66a1`); regenerated `docs/TO-DO.md`
with a v0.1.0 summary (39 done / 1 in-progress / 11 open / 1 held / 1
dropped / 2 needing owner confirmation); rewrote `README.md` for the real
11-crate workspace; corrected `CLAUDE.md`'s dependency-direction invariant
to name T-221's known exception; wrote `docs/EVALUATION.md` §6
(consolidated limitations) and §7 (§14 checklist scored item by item: 8
fully met, 1 partial, 2 not met); reworded one last dead-architecture doc
comment in `ferrite-servo/src/shell.rs` and dropped `check_purge.sh`'s
now-stale exclusion for it. Verified clean, full history: no AI attribution
anywhere (`git log --all --format="%H %B" | grep -iE "claude|anthropic"`
matches only two commits, both about the file named `CLAUDE.md`); no
`.rules`/`commands.md`/broker crates/9222 forward anywhere live;
`grep -rn "allow(dead_code)" crates/` — zero matches. `just check && just
test`: **228s (3m48s), exit 0, every crate green** — real headroom under
the 5-minute budget. `cargo deny check` clean (same pre-existing
Servo-source warning every agent since A3 has logged). Did **not** tag
`v0.1.0` (coordinator's call) and did **not** touch T-204 (held).

**What's left open, for whoever picks this up next:**
- **T-224 (the biggest one)** — the live app runs the pre-rebuild
  `GeminiAgent`/`BrowserTool` path, not the new `ferrite-model`/
  `ferrite-engine`/`browser_loop` stack this rebuild built and tested.
- **T-227** — corpus is 29 cases against a ~360 target; 0% double-authored,
  κ not computed. Needs a genuinely independent second author, not another
  solo session pretending to be one.
- **T-220** — `ServoEngine` (new path) doesn't complete real navigation;
  the live app's own, separate Servo integration is unaffected.
- **T-221** (dependency direction), **T-222/T-223** (UI: diff loses
  `(tool, origin)` pairing; no `Focusable` button in this iced version),
  **T-228** (one adjudication category's attribution check, root-caused
  not fixed), **T-209/T-210** (license choice, CI cadence — need the
  project owner's word before anything is pushed or tagged).

**Known next direction, not scoped here:** the user has indicated the next
phase of work after this rebuild is a polished-UX, feature-complete browser
effort. This charter does not scope that work — it's a future charter's
job to decide what "feature-complete" means and how it relates to closing
T-224 (arguably its first real task, since a UX effort presumably wants to
run on the stack this rebuild actually proved). Flagging the direction
plainly, not the plan.

**My honest assessment for the coordinator:** the doc layer is now accurate
and the hard invariants (no AI attribution, no dead architecture, no dead
code, green gates under budget) hold. Whether to tag `v0.1.0` given T-224's
live-wiring gap is a judgment call about what the tag is meant to promise —
see `docs/EVALUATION.md` §6 item 7 before deciding either way.
