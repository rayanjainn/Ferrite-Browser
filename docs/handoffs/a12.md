# Handoff — A12 (Harness + metrics)

**Session note:** same hazard A4–A11 hit — worktree started on a stale,
unrelated tree (`65b6d67`). Recreated `rebuild/a12-harness-metrics` from
local `main` (`7cc46f4`, carries A0–A11) before any work; nothing lost
(working tree was clean).

**Landed, all four mandatory priorities plus the metrics deliverable:**
T-215 (one-line `set_defense_mode` fix, done first), T-004/D4 (outcome
lattice: `Stripped`/`ContainedViaConsent`/`Executed`/`NotAttempted`,
table-driven test), T-010/D10 (swept `ConsentPolicy`, `reconsent()` for
post-hoc sweeps), the metrics module (Wilson CI, McNemar exact test,
Holm-Bonferroni, Cohen's h — every formula hand-checked, not just
"doesn't panic"), the report generator, `WorstCaseAgent` +
`examples/eval.rs` (`just eval` now runs the real 29-case corpus
end-to-end, zero live calls, verified twice locally: 29 cases, 96
executions, audit chain verifies both times), T-202's `inspect_case`
example, and `docs/EVALUATION.md`. Full detail, real numbers, and the
exact test names proving each claim are in `docs/PROGRESS.md`'s
2026-09-19 entry — read that, not this file, for the substance.

**Verified clean:** `cargo fmt --all --check`, `cargo clippy --workspace
--all-targets -- -D warnings`, `cargo machete`, `cargo test --workspace`
(every crate, 0 failures), `cargo deny check` (advisories/bans/licenses/
sources all `ok`). 10 commits on this branch, all real and buildable.

**Scope walls respected, not worked around:** `comparator/`, `fingerprint/`,
`sanitizer/`, `dry_run/`, `twin/` were read-only this session. This is
exactly why T-225/T-226 became documented limitations rather than schema
fixes (both real proposed fixes need those directories) and why T-228
(found this session, root-caused, not fixed) is left for whoever next
owns `adjudication.rs`'s `WithinFingerprintOriginShift` check.

**A13's exact next action** (reconciliation & release — all docs,
`CLAUDE.md`, `README.md`, version tagging):
1. Final pass asserting every doc claim in this repo against a real test
   name or commit SHA — `docs/EVALUATION.md` already does this for the
   eval-specific claims; extend the same discipline to `PROGRESS.md`'s
   older entries if any have drifted.
2. Regenerate `docs/TO-DO.md` — check `T-228` is still accurate (a fresh
   session should re-run `inspect_case` against `ref09_cat5_within_fingerprint.json`
   before trusting the root-cause trace is still current).
3. Tag `v0.1.0`.
4. Write the final, consolidated limitations section: irreducible blind
   spot, pattern ceiling, consent-policy upper bound (already precisely
   stated in `docs/EVALUATION.md` §2.5 — reuse, don't re-derive),
   single-machine eval, **and now: the real corpus-size shortfall against
   §13.3** (`docs/EVALUATION.md` §3 has the exact derivation-vs-reality
   reconciliation — fold it in rather than re-deriving it), **and the
   model-backend drift** (`docs/EVALUATION.md` §4 / T-224: the eval
   harness's fingerprint layer doesn't run through `ferrite_model`/Ollama
   at all — decide whether this is acceptable to ship v0.1.0 with, stated
   honestly, or blocks the tag).
5. `docs/TO-DO.md` T-204 is still `held` — do not act on it without a
   fresh, explicit go-ahead; this session did not touch it, per instruction.
