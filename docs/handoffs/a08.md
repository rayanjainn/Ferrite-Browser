# Handoff — A8 (Audit log)

**Session note:** same hazard A4–A7 hit — stale, unrelated worktree tree.
Recreated `rebuild/a08-audit-log` from local `main` (`dd5fd71`, A0–A7)
before any work.

**Landed:** `crates/ferrite-audit-log/src/lib.rs` — `compute_entry_hash`
is now the single function `AuditLog::append`/`verify_chain` both call,
hashing every persisted field (`schema_version` [new, =1], `entry_id`,
`sequence`, `timestamp`, `kind`, `principal_id`, `capability`, `url`,
`prev_hash`) via `serde_json` over a fixed-field-order `HashPreimage`
struct, prefixed with domain constant `b"ferrite-audit-v1"`. Closes
T-005/T-108: `capability`/`url` (where `EvalExecutionRecorded` carries
`exec_id`/`case_id`) can no longer be rewritten in SQLite with
`verify_chain()`/`load()` still reporting the chain intact.

`PersistentAuditLog` also gained a transactionally-written
`audit_chain_head` checkpoint (next sequence + last hash) so `load` can
detect a truncated tail — a hash chain alone can't prove completeness.
Best-effort watermark, not a guarantee: an attacker who forges the
checkpoint too defeats it — tested/documented explicitly, filed as
**T-218** for whoever owns the threat model.

**Cross-crate compile touches:** none — `append`/`verify_chain`/`new`/
`load` signatures unchanged; `AuditEntry` gained `schema_version: u8` but
no other crate constructs it by literal (checked `ferrite-servo`,
`ferrite-shell`, `ferrite-ui`, `ferrite-eval`).

**Tests:** 21 in the crate (0 before this session) — tamper matrix (one
test per persisted field, 10 tests, T-005's direct regression), plus
truncation, reordering, and insertion attack tests, plus a golden chain
fixture pinning two entry hashes, plus persistence round-trip sanity.
Full test-name list and pinned hex values are in `docs/PROGRESS.md`'s
2026-09-18 A8 entry — not repeated here to keep this file short.

**Verified:** `cargo test -p ferrite-audit-log` (21/21); `fmt --check`/
`clippy --all-targets -D warnings` clean. `cargo build|test --workspace`
green (no real `FAILED`/`error[` matches). `just check`/`just test`
green. `cargo deny check`: advisories/bans/licenses/sources ok, only the
pre-existing Servo-git-source warning (A3–A7's same one).

**On-disk audit DBs:** none exist in this repo (checked `git ls-files`
for `.db`/`.sqlite*` and a filesystem search excluding
`target/`/`.git/`). This session's breaking change to what
`verify_chain()` passing means invalidates nothing that exists today.

**Commit:** `6250cb3`.

**Known issues filed:** **T-218** (checkpoint is best-effort, needs an
external anchor for a real guarantee). **T-219**
(`AuditError::HashMismatch`/`ChainBroken` predate this session,
unconstructed since `verify_chain` returns `bool`; fixing needs a public
signature change touching 3 external call sites — real, out of scope
here).

**Exact next action for A9:** `crates/ferrite-engine`,
`crates/ferrite-engine-servo` do **not** exist yet (absent from root
`Cargo.toml` members and disk) — create both from scratch.
`crates/ferrite-agent` already exists as a workspace member.
`crates/ferrite-servo` also exists but is different — the winit/Iced
shell's own Servo integration behind its `servo` feature, not the
directive's `BrowserEngine`-trait `ferrite-engine-servo`. Read both
before naming or wiring anything.
