# Handoff — A3 (Model layer)

**Session note:** the first A3 attempt hit an opus rate limit mid-session
after 2 clean commits (89 tests green); the coordinating session finished
the remaining backends directly in the same worktree rather than
re-spawning. Everything below is independently verified against the actual
crate, not the interrupted session's self-report. Full detail in
`docs/PROGRESS.md`'s 2026-09-18 A3 entry.

**Landed:** `ModelProvider` trait, `ProviderId`/`ModelTier`/
`ProviderCapabilities`; trust-boundary `guard::finalize` (size → empty-body
→ parse, in that order); `MockProvider`, `ReplayProvider`, `OllamaProvider`
(cloud+local, one struct), `GeminiProvider`; `Cache`/`Throttle`/`Budget`
decorators (disk-backed content-addressed cache, semaphore+token-bucket+
jittered-backoff throttle tested via injected `Sleeper`+`ferrite-core::Clock`
— no real sleeps, budget with reserve-before-call rollback-on-rejection);
`ModelConfig` (T-213, env-sourced, no default model tags per §10.2);
`tests/conformance.rs` (exit-gate deliverable: identical assertions against
Mock and Replay through `&dyn ModelProvider`); 4 CLI examples + matching
`justfile` recipes (`models`/`probe`/`record`/`cache-stats`); secret-scan in
`scripts/hooks/pre-commit`; `deny.toml` updated for `keyring`/`dirs`'s new
duplicate-version crates.

**Two real bugs found and fixed, not just noted:**
1. `fixtures::FIXTURE_DIR` was a relative path constant that resolved
   wrong under plain `cargo test` (package dir, not workspace root, is the
   test-binary CWD) — replaced with `fixture_dir()` off
   `CARGO_MANIFEST_DIR`, with a regression test.
2. Neither backend exposed the raw wire body `just record` needs
   (`complete()` only returns the digested `CompletionResponse`) — added
   `fetch_wire()` to both, sharing the existing request path.

**Verified, not assumed:** `cargo test -p ferrite-model` (154 passing),
`cargo test --workspace` (everything else still green), `cargo clippy
--workspace --all-targets -D warnings` clean, `cargo fmt --all --check`
clean, `cargo deny check` exits 0, `cargo machete` clean, `just check`/
`just test` both green.

**Not done, stated plainly:** `just probe`/`just models`/`just record`
build and fail gracefully with no credentials, but have never made a real
network call — there's no `OLLAMA_API_KEY` in this sandbox. A3's exit gate
literally asks a human to run `just probe` once; that's still open. The two
committed fixtures are hand-written-JSON-shaped, not real recordings.

**One process note, not a code issue:** commit `3dcaba5`'s message says
"examples added in the next commit" — there was no next commit (`git add
-A` staged everything together). Content is correct; that one sentence in
the message isn't. Recorded in `docs/PROGRESS.md` rather than silently
left, per R2.

**Exact next action for A4:** `crates/ferrite-ipi/src/fingerprint/` per §6/A4.
Depend on `ferrite-model` (`ModelProvider`, not a concrete backend — §10.5)
and `ferrite-core` (the capability taxonomy, `ScopablePrimitive` for the
structural js.execute exclusion). The adversarial six-condition matrix
(§10.4) is already proven at the transport level in `ferrite-model`'s own
tests; A4's job is the *policy* layer on top — filtering a parsed `may_use`
prediction against the closed 7-capability allowlist — which this crate
deliberately left undone (see `ferrite-model`'s lib.rs module doc, "Trust
boundary" section).
