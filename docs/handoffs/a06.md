# Handoff — A6 (Dry-run + twin + containment)

**Session note:** same hazard A4/A5 hit — worktree started on the
pre-rebuild tree (`65b6d67`, unrelated "Task 17"/Git-LFS history).
Recreated `rebuild/a06-dry-run-twin-containment` from local `main`
(`b229fb1`, carries A0–A5) before doing any work.

**Landed:** `crates/ferrite-ipi/src/dry_run/{mod,record,content,executor,
orchestrator}.rs` and `src/twin/{mod,key,crypto,manager}.rs` replace the
flat `dry_run.rs`/`twin.rs`. `containment.rs` deleted outright (T-007/D7
closed — by-construction, grep-backed test proves `RecordingExecutor`
names no network dependency). `twin`'s `DEV_KEY` constant is gone
(T-008/D8) — keyring then `FERRITE_TWIN_KEY`, reusing `ferrite-model`'s
`SecretStore`/`OsKeyring`/`Token`/`EnvSource`, never a new wrapper. `nix`
and its dead `cargo-deny` skip entry removed.

**Two deliberate deviations (full reasoning in `docs/PROGRESS.md`'s
2026-09-18 A6 entry):**
1. `ToolEvent` keeps `tool: ToolId`/`origin: Option<String>`, not the
   directive's illustrative `primitive`/`seq` — `comparator.rs`/
   `dataset.rs` (A7/A11, off-limits) construct/match the old shape and
   wouldn't compile otherwise. Added `ToolEvent::primitive()` (tested
   bridge to `ferrite_core::Primitive`) and
   `DryRunRecord::events_with_seq()` (derived seq via `Vec` position).
2. `DryRunOrchestrator::run()` does **not** hard-fail on a missing twin
   key — found empirically, it broke 5 `ferrite-eval` tests assuming no
   operator secret is needed for the suite (R7's spirit, never stated for
   this key before). `TwinManager::load_or_generate` still returns the
   strict, tested `Result::Err`; `run()` catches it, logs a warning, and
   generates an unpersisted twin instead — the key gates a cache, not the
   containment/sanitizer/consent mechanism.

**T-215/T-003, this session's half:** `DryRunOrchestrator::set_defense_mode
(DefenseMode)` derives `detect_enabled`/`strip_enabled` together, reusing
A5's `DefenseMode::sanitizer_detect_enabled`/`sanitizer_strip_enabled`.
**A12's exact fix:** in `ferrite-eval/src/harness.rs::run_one` (~line
220), replace `orch.set_detect_enabled(behavior.detect_enabled);` with
`orch.set_defense_mode(mode);` (`mode` is already a parameter; nothing
later reads `behavior.detect_enabled` again). Same shape of fix needed in
`ferrite-ui/src/lib.rs`.

**Verified:** `cargo test -p ferrite-ipi` (140 passed, 2 doctests),
`fmt`/`clippy --all-targets -D warnings`/`machete` clean. `cargo build
--workspace`/`cargo test --workspace` green (0 failed) — required
bounding `TwinManager`'s `Box<dyn EnvSource>` as `+ Send + Sync`
explicitly (the trait itself carries no such supertrait;
`ferrite-ui` holds an orchestrator across an `.await` in a spawned task).
`just check`/`just test` green from repo root. `cargo deny check` clean
except the pre-existing Servo-git-source warning.

**Commits:** `be891b8`, `f8edef3`.

**Exact next action for A7:** `crates/ferrite-ipi/src/comparator/`
per §6/A7 — the highest-value fix in the rebuild (T-001/T-002). Consumes
`DryRunRecord.tool_events: Vec<ToolEvent>` (`{tool: ToolId, origin:
Option<String>}`, ordered by `Vec` position; `events_with_seq()` for an
index, `.primitive()` per-event for a best-effort `ferrite_core::Primitive`
— `None` for `download.file`, see T-216). Opaque-scheme origins
(`about:blank`, `data:`) are recorded literally and will fail
`Origin::parse` — see T-211, likely yours to resolve (an
`Origin::Opaque` variant, or ratify parse-failure-as-signal).
