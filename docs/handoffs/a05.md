# Handoff — A5 (Sanitizer)

**Session note:** same hazard A4 hit — worktree branch was initially the
pre-rebuild tree (`Browser/`-nested, unrelated commit history: "Task 17",
"Git LFS", etc.). Recreated `rebuild/a05-sanitizer` from local `main`
(`97c7164`, carries A0–A4) first. Check `git log main --oneline` if a
future worktree looks like the old tree — not a repo regression.

**Landed:** `crates/ferrite-ipi/src/sanitizer/{mod,patterns,detect,html,
excise,config}.rs` replaces the old flat `sanitizer.rs` (module-path
conflict). Pattern set is versioned data (`PatternDef`/`PatternSet`,
stable IDs, `since_version`), 5 general + 5 script patterns (same counts
as before). Detection covers all 3 carriers (visible text post-`ammonia`,
comments pre-`ammonia`, JSON leaves with path tagging) plus script
content. Excision is sentence-segment, tag-boundary-safe, single-space-
only, with an HTML-validity property test using `ammonia::clean()`'s own
parse/serialize as the well-formedness oracle (12 fixtures).
`SanitizerConfig`/`run()` makes detect/strip independently toggleable and
actually wires excision — no more "detect then forget to strip". False-
strip rate measured over 35 benign fixtures vs. a provisional 2% ceiling
(T-203, provisional pending A11's real audit).

**Minimal, charter-sanctioned touch to `tool_decision/mod.rs`:** added
`DefenseMode::sanitizer_detect_enabled`/`sanitizer_strip_enabled`; changed
`prepare_task`'s `On`/`SanitizerOnly` arms to call `sanitizer::run` instead
of the old detect-only `sanitize_html` — a second, previously unnamed
instance of D3's symptom, found and fixed at this call site.

**T-003 only PARTIALLY closed — read before treating it as done.** The
literal `strip_enabled` field D3/T-003 names lives in
`crate::dry_run::RecordingExecutor`/`DryRunOrchestrator`, out of this
charter's scope. Still defaults `false`; neither `harness.rs::run_one`
(sets `detect_enabled`, never `strip_enabled`) nor `ferrite-ui/src/lib.rs`
(sets neither) ever flips it — the dry-run path producing
`ExecutionRecord`s/metrics still runs with excision off in every mode.
Filed as **T-215**, owned by A6 (`dry_run.rs`) + A12 (`harness.rs`).
`docs/TO-DO.md` marks T-003 `in-progress`, not `done`.

**Verified:** `cargo test -p ferrite-ipi` — 142 unit + 2 doctests, 0
failures (up from A4's logged 123). `cargo fmt`/`clippy --all-targets -D
warnings` clean for the crate. `just check` and `just test` clean
workspace-wide (proves `ferrite-eval`/`ferrite-ui` still compile against
the rebuilt API). `cargo deny check` clean (same pre-existing Servo-
git-source warning A3/A4 also saw).

**Compatibility fix mid-work:** `dry_run.rs`'s test `w1_t1a_comment_caught`
pins the exact string `"instruction_override"`. Rather than rename it
(breaking that off-limits file), "ignore" kept that bare id; only
"disregard" got a new id (`instruction_override_disregard`).

**Commits:** `58c9121`.

**Exact next action for A6:** `crates/ferrite-ipi/src/dry_run/`, `twin/`,
`containment/` per §6/A6; legacy `dry_run.rs`/`twin.rs`/`containment.rs`
are reference material. Two known, unfixed issues: (1) `twin.rs` has a
real hardcoded `DEV_KEY` — must become keyring/env-backed, fail loudly, no
constant (T-008). (2) **T-215** — give `DryRunOrchestrator` a real
`strip_enabled` derivation (mirror `sanitizer::run`'s config, or reuse
`DefenseMode::sanitizer_strip_enabled`) and make whatever replaces
`harness.rs::run_one` actually call it — the other half of T-003.
