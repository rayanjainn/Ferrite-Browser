# Handoff — B3 (`ferrite-ui`/`ferrite-shell` live wiring)

**Landed (`555ad99`+`3addb46`+`c696d0b`):** T-224/T-229/T-220 closed. The
live `ferrite-ui` app now constructs a real `ferrite_model::ModelProvider`
at startup (`launch()`, never `Default::default()` — R7), the dry run drives
`ferrite_agent::browser_loop::run_agent_loop` directly against
`ferrite_ipi::dry_run::DryRunEngine` (no bridge), and the live run drives a
new message-driven step loop against `ferrite_engine_servo::
BorrowedServoEngine<'a>` — a `BrowserEngine` face over the tab's own,
already-correctly-driven `HeadlessServoSession` (T-220's fix). The old
`GeminiAgent`/`BrowserTool`/`AgentRuntime`/`ToolExecutor` vocabulary is
deleted workspace-wide (grep-confirmed zero real references before
deletion); `ferrite-agent` now exports only `browser_loop`.

**Exact next action for B4 (final full-workspace verification before the
coordinator hands the app to the user):**

1. **Confirm every crate's tests green together** — `cargo test --workspace`
   should show the same ~30 green test-binary blocks this session's
   `docs/PROGRESS.md` B3 entry cites, plus `ferrite-ui` at 29 (up from 17)
   and `ferrite-agent` at 7 (down from 15). `cargo build --workspace`,
   `cargo fmt --all --check`, `cargo clippy --workspace --all-targets -D
   warnings`, `cargo deny check` should all still be clean.

2. **App launch with the real Servo feature — already confirmed this
   session, re-verify rather than re-attempt from scratch.**
   `cargo build -p ferrite-shell --features ferrite-servo/servo` succeeded
   (26m06s). `cargo run -p ferrite-shell --features ferrite-servo/servo --
   ui` launched with the real Servo engine (no "Servo unavailable" message)
   and stayed running with no crash; re-run with
   `FERRITE_MODEL_SMALL=FERRITE_MODEL_MAIN=gemma3:27b` (this sandbox's own
   `"ferrite"`-service OS-keyring credential) printed no warnings at all —
   a real `ModelProvider` and the real Servo session both constructed
   successfully in the same process. B4 should sanity-re-check this still
   holds (a quick relaunch, not a full rebuild) rather than assume it's
   permanent, and go straight to item 4 below (the actual click-through
   gap) since construction-level verification is already done.

3. **A full-workspace grep for old-vocabulary references** — this session
   already did this before deleting (`docs/PROGRESS.md`'s B3 entry lists
   the exact grep and its all-comment result), but B4 should re-run it
   fresh as its own independent check per this project's established
   "verify, don't trust the prior session's own claim" practice (A3/B2's
   precedent): `grep -rEn "ferrite_agent::(BrowserTool|AgentRuntime|
   GeminiAgent|ToolExecutor)" --include="*.rs" crates/` should show only
   doc-comment matches, none in a real `use`/type position.

4. **If a real `FERRITE_MODEL_SMALL`/`FERRITE_MODEL_MAIN` + a display are
   available**, exercise the live app for real: submit an agent task,
   confirm the dry-run/consent gate still fires correctly on a
   deliberately-suspicious prompt, approve/reject an item, confirm the live
   run then executes only the approved actions. This session could not do
   this (no display, no model env vars exported in its own shell even
   though an OS-keyring credential exists under service `"ferrite"`) — see
   `docs/PROGRESS.md`'s B3 entry, "Honest remainder," for the precise gap.

5. **Update `README.md`/`CLAUDE.md`** if this charter's changes make
   anything there stale — in particular `CLAUDE.md`'s dependency-direction
   invariant still names the now-closed T-221 `ferrite-ipi → ferrite-agent`
   exception (stale even before B3; B3 did not touch `CLAUDE.md`, out of
   this charter's own scope, but B4 should notice it while doing item 5).

Do not attempt T-204 (held). Do not merge to `main` — that's the
coordinator's call once B4's pass is green.
