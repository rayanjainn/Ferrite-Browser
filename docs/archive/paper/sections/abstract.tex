% ============================================================
% Abstract
% ============================================================
% Target length: 150–200 words
% Write LAST — after all other sections are complete
% Must cover: problem, approach, key results, contribution
% ============================================================

\begin{abstract}

\todo{Write abstract last, after evaluation data is available.
      Must include: (1) the problem in one sentence,
      (2) our approach in two sentences,
      (3) three key quantitative results,
      (4) the contribution claim.
      Target: 175 words.}

% DRAFT TEMPLATE (fill in numbers from evaluation):
%
% AI agents increasingly browse the web autonomously, yet no browser
% enforces formal access control over agent actions---creating a governance
% vacuum exploited by prompt injection attacks in 2025.
% We present \ferrite{}, the first browser designed from the ground up
% to treat AI agents as first-class principals with capability-based
% access control and cryptographically verifiable audit trails.
% \ferrite{} enforces a single architectural principle: every privileged
% action---by extensions, agents, or web content---requires an unforgeable
% capability token evaluated against composable Rego policies and recorded
% in a tamper-evident hash-chained log.
% Our evaluation shows \ferrite{} achieves median broker latency of
% \todo{X}$\mu$s, blocks 100\% of \todo{N} adversarial scenarios including
% four real-world 2025 attacks, and reduces bandwidth by \todo{X}\% on
% tracker-heavy sites.
% \ferrite{} is open-source, built in Rust on the Servo engine, and
% demonstrates that architectural enforcement provides a complementary
% defense layer independent of model-level prompt injection mitigations.

\end{abstract}
