# Ferrite — Task Ledger

Stable `T-###` IDs. IDs are never reused or renumbered — a dropped task
keeps its ID with `dropped <reason>`. Derived by Agent A0 from three
sources, per `docs/REBUILD_DIRECTIVE.md` §6/A0:

1. **D1–D14** — the defect register (`docs/REBUILD_DIRECTIVE.md` §9),
   T-001 through T-014, one-to-one.
2. **A1–A13** — the agent charters (`docs/REBUILD_DIRECTIVE.md` §6),
   T-101 through T-113, one-to-one.
3. **The old `docs/archive/TO-DO.md`'s still-open items** — T-201 and up.
   Everything from that 5,909-line file was reviewed; completed work
   (Tasks 1–17, 20a–20d, W1–W6, Phase 0) is not carried forward as a task
   (it's done — see `docs/archive/PROGRESS.md` for the record, and
   `docs/DECISIONS.md` for what of it became a live ADR). What was
   genuinely still open gets a T-2xx entry below, or an explicit
   `dropped <reason>` if the rebuild directive supersedes it.

Status enum: `open` / `in-progress` / `done <SHA>` / `dropped <reason>` /
`held <reason>`.

---

## Defect register (D1–D14 → T-001–T-014)

| ID | Status | Owning charter | Defect | Fix |
|---|---|---|---|---|
| T-001 | done `6cb4c70` | A7 | `compare()` takes one global `OriginScope` for the whole task; per-capability scoping from the design docs (ADR-004) is inexpressible | `crates/ferrite-ipi/src/comparator/`: `compare(expected: &ExpectedFingerprint, actual: &DryRunRecord)`, `ExpectedFingerprint` wraps `ferrite_core::ExpectedCapabilitySet`; lowering via `ExpectedCapabilitySet::lowered() -> Vec<(ScopablePrimitive, &OriginScope, Capability)>`. Proven by `comparator::tests::narrow_scoped_read_and_wide_web_read_in_the_same_fingerprint_attribute_independently`. |
| T-002 | done `6cb4c70` | A7 | `OriginScope::admission_rank()` is computed and never consumed — dead code | `compare` scans every candidate capability and keeps the one with the maximum `Specificity` (`OriginScope::admits`'s return value), recording it on `Attribution`. `comparator::tests::admission_rank_is_actually_consumed_not_just_computed` and `..._picks_the_tighter_of_two_admitting_capabilities` are written to fail on a "first match wins" regression. |
| T-003 | in-progress `58c9121`+`be891b8` | A5 + A6 | Sanitizer excision built, tested, off everywhere (`strip_enabled` never set `true`) → `On` and `LoopOnly` are behaviorally identical in production despite being presented as distinct conditions | **Still not fully closed — one caller-side change left, see T-215.** A5 rebuilt the sanitizer with live, config-gated excision and wired it at `tool_decision::prepare_task`. A6 gave `crate::dry_run::DryRunOrchestrator` a real, tested `set_defense_mode` that derives `strip_enabled` correctly from `DefenseMode` — the mechanism is now fully real and correct on the `ferrite-ipi` side. What's left: `ferrite-eval/src/harness.rs::run_one` and `ferrite-ui/src/lib.rs` still call the older `set_detect_enabled` alone instead of the new `set_defense_mode`, so the dry-run path they drive still runs with excision off in every mode. Both are one-line fixes — see T-215's exact wording. |
| T-004 | open | A12 | `final_outcome` in `On` mode ignores `sanitizer_caught`; once excision is live, a stripped attack reports as `Executed` — inverting the headline metric | New outcome lattice: `Stripped` / `ContainedViaConsent` / `Executed` / `NotAttempted`, table-driven test over every `(caught, gated)` pair. Depends on T-003. |
| T-005 | done `6250cb3` | A8 | Audit hash omits `capability`/`url`; `exec_id`/`case_id` payload can be rewritten with `verify_chain()` still passing | `crates/ferrite-audit-log/src/lib.rs`: `compute_entry_hash` is now the single function `AuditLog::append`/`verify_chain` both call, covering every persisted field (including `entry_id` and a new `schema_version` byte) via a canonical `serde_json` serialization of `HashPreimage`, domain-separated with `b"ferrite-audit-v1"`. Tamper matrix proves all 9 fields, not just the 2 named. |
| T-006 | done `6e6926a` | A11 | `CarrierVector` partition validated only at JSON-corpus-load time; hand-built Rust literals bypass it | `CarrierVector` is now `WebContent(WebContentVector)` / `ToolOutput(ToolOutputVector)` — an enum-of-enums, not two independent `CaseDefinition` fields. The mismatched pairing is not a value that exists, in Rust or JSON; `corpus.rs`'s old `partition_matches`/`CorpusError::Partition` are deleted, not superseded. Proven by a `compile_fail` doctest on `CarrierVector` plus `corpus::tests::old_flat_carrier_shape_fails_to_deserialize_not_a_partition_check`. |
| T-007 | done `be891b8` | A6 | `intercept_request()` unreachable outside its own test — containment is by mock (RecordingExecutor never makes real calls), not by interception | Deleted `crates/ferrite-ipi/src/containment.rs` outright (option (a), the directive's default) plus the now-dead `nix` dependency and its `cargo-deny` skip entry. `crate::dry_run::executor` documents "containment is by construction" and backs it with a grep-based test (`this_modules_source_names_no_network_capable_dependency`) that `RecordingExecutor`'s own source names no network-capable dependency. No evidence found of a near-term real-executor dry-run backend plan (checked `docs/REBUILD_DIRECTIVE.md` and this file) — A9's `ServoEngine` is for the *production* agent loop, not the dry run. |
| T-008 | done `be891b8` | A6 | Twin AES key is a compiled-in constant (`ferrite-ipi-twin-dev-key-32byte!`) | `crates/ferrite-ipi/src/twin/key.rs` — OS keyring first, `FERRITE_TWIN_KEY` env var second, typed `TwinKeyError` (not a panic) when neither is present, reusing `ferrite-model`'s `SecretStore`/`OsKeyring`/`Token`/`EnvSource` rather than a new keyring wrapper. `TwinManager::load_or_generate` returns that error directly (tested). One caveat: `DryRunOrchestrator::run` (the layer `ferrite-eval`/`ferrite-ui` call) does *not* propagate that error out of the whole dry run — it logs a warning and generates an unpersisted twin instead, so a missing key degrades caching, not the defense loop itself. See `docs/PROGRESS.md`'s 2026-09-18 A6 entry and that method's doc comment for the full reasoning. |
| T-009 | done `6e6926a` | A11 | AgentDojo adapter is enum plumbing only (`Tier3AgentDojo`, `RunLabel::R9`), no Slack-suite mapping exists | Implemented for real: `ferrite_eval::agentdojo` adapts 3 real, cited AgentDojo Slack-suite injection tasks onto Ferrite's browser tool surface via a documented tool-substitution mapping (no Slack-shaped tools added). Output round-trips through the real `corpus::load_case`. Honest scope: 3 of AgentDojo's ~140 possible task pairings, not a suite port — see T-227. |
| T-010 | open | A12 | Simulated consent user hard-pinned to `RejectFlagged` | Make it a swept parameter (`RejectFlagged` / `ApproveAll` / `RandomP(p)`); report `RejectFlagged` explicitly as an upper bound on human vigilance, not an estimate |
| T-011 | done `01c3f54` | A0 | `CLAUDE.md` listed five completed tasks as "not yet implemented" (commit `46b2177` built them and edited the same file without fixing the claim) | Rewrote CLAUDE.md to the zero-status contract; `scripts/check_no_archive_links.sh` now fails CI if a status heading reappears |
| T-012 | done `a45b2ad`+`01c3f54`+`6ccfdad` | A0 | `.rules`/`README.md`/`commands.md`/`devcontainer.json` described the dead broker architecture; `.rules` is auto-ingested by AI tools — self-inflicted context poisoning | Deleted `.rules`/`commands.md`; rewrote `README.md`, `devcontainer.json`, `Dockerfile` |
| T-013 | done `af8d8e3` | A1 | Docs assume Windows/PowerShell/single dev machine; reality is multi-OS, multi-contributor | `justfile` added as the only documented command surface (OS-neutral, verified: `just check`/`just disk`/`just test-fast`/`just eval`/`just install-hooks` all actually run on this machine) |
| T-014 | done `9e3f097`+`ffe3c63` | A0 | `paper/PAPER_STATUS.md` tracks past deadlines against a track the engineering TO-DO says is parked | Archived to `docs/archive/paper/` with a banner noting the deadlines are stale and the track is parked |

---

## Agent charters (A1–A13 → T-101–T-113)

One row per charter. Each charter's own exit gate (`docs/REBUILD_DIRECTIVE.md`
§6) is the completion criterion — not repeated here in full.

| ID | Status | Charter | One-line scope |
|---|---|---|---|
| T-101 | done `af8d8e3`+`8b9d665`+`9fee225` | A1 — Foundation | `[workspace.dependencies]` (`8b9d665`... actually `chore(build): consolidate` commit), build profiles, `justfile`, `deny.toml`, CI matrix rework (Servo weekly not every push, widened to ubuntu/macos/windows) — done. Not done: `.cargo/config.toml`'s target-dir deliberately left to the justfile instead (documented deviation, see that file); `cargo-bloat`/`cargo-sweep` not installed (recipes exist, tools aren't — low priority, install on first real need). |
| T-102 | done `1c01a3e`+`2a19b00`+`01e392e`+`eb80812`+`9679904` (reviewed and merged to `main` at `b1545d3`) | A2 — Core contracts | `ferrite-core` landed: closed 7-capability/17-primitive taxonomy with the lowering as a `const LOWERING` table and a compile-time exhaustiveness gate (verified: an unassigned primitive produces `error[E0004]`); `js.execute` made structurally unrepresentable on the expected side via a separate 16-variant `ScopablePrimitive` (+ a `compile_fail` doctest); per-capability `OriginScope` with `admits() -> Option<Specificity>` and `ExpectedCapabilitySet::lowered()` yielding `(ScopablePrimitive, &OriginScope, Capability)` — the type T-001/T-002 need; newtype IDs; `Clock`/`SystemClock`/gated `FixedClock`; `thiserror` errors; serde round-trip + golden schema fixtures. **Not done:** config loading — nothing in this layer needs a config value, deferred to A3 as T-213. |
| T-103 | done `75ba980`+`d9f9438`+`3dcaba5`+`3062977` (reviewed and merged to `main` at `5bd0dc1`) | A3 — Model layer | `ferrite-model` landed in full: `ModelProvider` trait + `ProviderId`/`ModelTier`/`ProviderCapabilities`; trust-boundary `guard::finalize` (size ceiling → empty-body → JSON parse, in that order); four backends (Mock/Replay/Ollama/Gemini — Ollama covers cloud+local as one struct); three decorators (Cache: disk-backed, content-addressed, atomic write via temp+rename; Throttle: semaphore+token-bucket+jittered backoff+`Retry-After`, tested via an injected `Sleeper`+`ferrite-core::Clock` rather than real sleeps; Budget: reserve-before-call, rolls back on rejection); `tests/conformance.rs` (the exit-gate deliverable — same assertions against `MockProvider` and `ReplayProvider` through `&dyn ModelProvider`); CLI examples (`models`/`probe`/`record`/`cache_stats`) + matching `justfile` recipes; secret-scan addition to `scripts/hooks/pre-commit`. 154 tests in the crate (151 unit + 3 conformance). **Not verified live:** `just models`/`just probe`/`just record` all build and fail gracefully without credentials, but no `OLLAMA_API_KEY` exists in this sandbox to run a real round-trip — needs a human, see `docs/handoffs/a3.md`. |
| T-104 | done `d9e2c12` | A4 — Fingerprint engine | `crates/ferrite-ipi/src/fingerprint/{mod,rules,engine}.rs` landed: deterministic keyword `must_use` (retyped from `tool_decision::rule_based_must_use` against `ferrite_core::Capability`) plus a model-predicted `may_use` via `&dyn ferrite_model::ModelProvider`, filtered against `Capability::ALL` by exact string match. `Fingerprint::new` (private, the only non-empty constructor) subtracts `must_use` from the candidate `may_use` so the two sets are disjoint by construction. `predict_may_use`'s two `let-else` guards make fail-to-empty a control-flow property: every provider error returns before the insertion step is reachable. `js.execute` inherits `ferrite_core::Capability`'s structural absence of any `Execute`-class variant rather than re-deriving it — proven by a `compile_fail` doctest on `Capability::JsExecute` plus a unit test under the most permissive input the engine can produce. 123 tests in the crate (28 new), `just check`/`just test` green workspace-wide, `cargo deny check` clean. **Not done, explicitly deferred:** wiring `Fingerprint` into `ferrite_core::ExpectedCapabilitySet` (needs an authored `OriginScope` per capability, which is ADR-004/A7's data, not derivable from a prompt) — left for A7's comparator-integration; no test against `MockProvider::push_hang()` (that per-request timeout is `ferrite-model`'s `Throttle`, already proven in A3's suite). See `docs/handoffs/a04.md`. |
| T-105 | done `58c9121` | A5 — Sanitizer | Pattern set as versioned data (`crates/ferrite-ipi/src/sanitizer/{patterns,detect,html,excise,config}.rs`); live excision wiring at `tool_decision::prepare_task`. Only partially closes T-003 — see that row and T-215. |
| T-106 | done `be891b8`+`f8edef3` | A6 — Dry-run + twin + containment | Ordered event log (`crates/ferrite-ipi/src/dry_run/{mod,record,content,executor,orchestrator}.rs`), keyring-backed twin key (closes T-008), containment deleted (closes T-007). `ToolEvent` keeps `tool: ToolId`/`origin: Option<String>` rather than the directive's illustrative `primitive`/`seq` field names — `crate::comparator`/`crate::dataset` (A7/A11, off-limits this session) already construct/match the old shape directly; `ToolEvent::primitive()` and `DryRunRecord::events_with_seq()` are the tested, non-breaking bridges. See `docs/handoffs/a06.md`. |
| T-107 | done `6cb4c70`+`40221de` | A7 — Comparator + origin model | Per-capability scoping, admission-rank attribution. Closes T-001, T-002, T-206. See `docs/PROGRESS.md`'s 2026-09-18 A7 entry and `docs/handoffs/a07.md`. |
| T-108 | done `6250cb3` | A8 — Audit log | Full-payload hash preimage, tamper matrix. Closes T-005. See `docs/PROGRESS.md`'s 2026-09-18 A8 entry and `docs/handoffs/a08.md`. |
| T-109 | done `aaae747`+`8a1b7e3`+`3df9af2` | A9 — Engine + agent action surface | `crates/ferrite-engine` (`BrowserEngine` trait, `MockEngine`, 24 conformance tests), `crates/ferrite-engine-servo` (`ServoEngine` wrapping `ferrite_servo::session::HeadlessServoSession`, feature `engine-servo`), `crates/ferrite-agent/src/browser_loop.rs` (new, additive plan→act→observe loop, engine/provider-agnostic, 8 tests). `just build-servo` succeeded for real this session (15m31s, 6.4GB target-dir — `docs/BUILD_BUDGET.md`'s A9 entry). The real-Servo conformance run got construction working but found real page navigation does not complete without a genuine winit event loop driving `HeadlessServoSession` — filed as **T-220**, not fixed (out of this charter's file scope). See `docs/handoffs/a09.md` and `docs/PROGRESS.md`'s A9 entry for full detail. |
| T-110 | done `aaec147` | A10 — UI/UX | Iced consent panel decoupled from page content, per-item approve/reject, UI state-machine tests |
| T-111 | in-progress `6e6926a`+`2bc1925`+`d1531c0` | A11 — Dataset + corpus | Mandatory priorities 1-3 (T-006, T-009, T-208) are genuinely complete. Priority 4 (real corpus construction per §13.3) is honestly partial: 29 of ~360 target cases (Tier1 17, Tier2 9, Tier3AgentDojo 3, benign 7 of those), 0% double-authored against the 10% requirement, κ not computable. Remainder filed as T-227. Absorbs T-201/T-203/T-208, all closed below. |
| T-112 | open | A12 — Harness + metrics | 4-mode matrix in code, corrected outcome lattice (closes T-004), consent-policy sweep (closes T-010), Wilson/McNemar metrics module, `docs/EVALUATION.md`. Absorbs T-202/T-204 below. |
| T-113 | open | A13 — Reconciliation & release | Final doc-vs-test/SHA audit, `v0.1.0` tag, honest limitations section |

---

## Additional items from the old TO-DO.md, not otherwise covered (T-2xx)

| ID | Status | Feeds | Item |
|---|---|---|---|
| T-201 | done `d1531c0` | A11 | **Corpus-authoring design document** (old Phase 1). Case anatomy, closed vocabularies, worked T1a/T1b examples, written against the *rebuilt* `CaseDefinition` (post T-006), not the pre-rebuild struct. Prerequisite for corpus construction, not optional preamble. Extended `crates/ferrite-eval/tests/pilot_corpus/CORPUS_AUTHORING_GUIDE.md` in place (established, already-referenced location) rather than a new top-level doc; fixed real pre-existing drift found while re-verifying against source (stale flat `OriginScope`/`carrier` shapes), added a T1b worked example, and replaced the old AgentDojo "not covered yet" placeholder with a real section. |
| T-202 | open | A12 | **Per-case result inspector** (old Phase 2 item 1): a CLI that runs one authored case and renders its per-mode records legibly (which modes ran, each layer's caught/missed, final outcome, match against authored `ground_truth`). Distinct from the corpus-wide metrics aggregator (T-112) — this is the "how do I see my one case's output" tool, needed *during* authoring, before there's a corpus-worth of data to aggregate. |
| T-203 | done (audit performed; findings T-225/T-226) | A11 | **Schema-completeness audit before authoring at scale** (old Phase 2 item 2): walked every metric formula in `docs/REBUILD_DIRECTIVE.md` §13.2 against the rebuilt `ExecutionRecord`/`CaseDefinition`. Most metrics (ASR, CR, ADR, SDR, FGR, UP, R, ΔL) are directly supported. Two real gaps found and filed, not silently passed: FSR is only a case-level proxy in `ExecutionRecord`, not the literal segment-level ratio §13.2 defines (T-225); no field anywhere carries model token usage, so ΔT cannot be computed from persisted data even though `ferrite_model::response::TokenUsage` already exists one crate over (T-226). Also re-affirmed (not newly locked) `sanitizer/config.rs`'s `PROVISIONAL_FALSE_STRIP_RATE_CEILING` (0.02): measured 0/35 (0.0%) on the current benign fixture set, still provisional pending the full ~100-case benign corpus §13.3 asks for (this session's corpus has 7 benign cases). Full table in `docs/PROGRESS.md`'s A11 entry. |
| T-204 | held — per prior explicit user instruction ("not now") | A12/A13 | **Freeze the defense and run the full R1–R10 / A1–A5 experiment matrix** (old Phase 6). Carries forward as a real task, not silently dropped, but must not be auto-run at any charter's exit gate without a fresh go-ahead — the hold was an explicit decision, not a scheduling gap. |
| T-205 | dropped — architecture deleted | — | Extism/Wasm extension sandbox and its demo extension (old Task 3, `extensions/hello-ext`, `ferrite-sandbox`). Deleted per `docs/REBUILD_DIRECTIVE.md` §4 (commit `a45b2ad`). `ferrite-engine`'s `MockEngine`-first design (T-109) replaces the sandboxing concern's original motivation (a safe place to run untrusted code) with a different mechanism (a safe place to run the *agent*, via the dry-run loop) — not a gap that needs separately re-filling. |
| T-206 | done `6cb4c70` | A7 | **`compare()` double-flag de-dup**: an out-of-scope-origin event whose primitive is also outside the expected union currently increments both `out_of_scope_origins` and `extra_primitives`. Predates the D1–D14 list (found in the pre-rebuild `PROGRESS.md`'s own Known Issues); recorded here so it isn't lost, resolved as part of T-107's rewrite of the same function rather than as a standalone patch to code being replaced anyway. Fixed by making "primitive named by some capability" vs. "by no capability" a strict partition of the primitive space (independent of origin), so classification is `if`/`else`, never both — no `Both` variant added, see `comparator`'s module docs for why. Test: `comparator::tests::t206_regression_primitive_not_expected_and_origin_not_admitted_lands_in_exactly_one_bucket`. |
| T-207 | done `42401fd`+`068fd8e` | A1 | **Workspace `cargo fmt`/`clippy --all-targets` gap — fully closed.** Fixed via `cargo fmt --all` (whitespace only, zero behavior change) plus documented `#[allow(clippy::await_holding_lock)]` on the 10 `ENV_GUARD`-across-`.await` test call sites in `ferrite-eval` (real root cause: the OLD CI's `cargo clippy --workspace` lacked `--all-targets` and so never linted test code at all — now fixed in `.github/workflows/ci.yml`, T-101). Verified: `cargo fmt --all --check` and `cargo clippy --workspace --all-targets -- -D warnings` both clean workspace-wide, `cargo test --workspace` green throughout. |
| T-208 | done `6e6926a` | A11 | **Pilot corpus is a real, reusable seed — not grandfathered.** The 10 cases in `crates/ferrite-eval/tests/pilot_corpus/` (plus `CORPUS_AUTHORING_GUIDE.md`/`PILOT_CORPUS_REFERENCE.md`) should seed the real corpus, but every case must be: (a) re-labelled under the post-rebuild `GroundTruth` enum as it exists once T-006/T-111 land, (b) re-validated against the type-level carrier partition (T-006), and (c) counted toward the 10% double-authoring / Cohen's κ ≥ 0.8 requirement (§13.3) like any newly authored case — no exemption for being pre-existing. All 10 reshaped to the new `carrier_vector` schema (mechanical only — `GroundTruth` itself was untouched by T-006, so no relabeling was needed) and re-validated: `pilot_corpus_validate.rs`'s existing 3 tests (unmodified) still pass. Counted toward the corpus-size denominator (29 total, see T-227) but not toward the double-authored numerator — all 10 are `Author::SelfAuthored`, single-authored, same as before. |

---

| T-209 | **needs owner confirmation** | — | A1 added `license = "MIT OR Apache-2.0"` and `publish = false` to all 7 crate manifests (commit `8b9d665`) because `cargo deny check licenses` requires a license field and none existed. The dual-license choice came from the old (now-archived) `.rules` file's stated intent ("License: Dual MIT/Apache-2.0"), not a fresh decision this session made or confirmed with the project owner. If that's not the intended license, change it before this is ever published anywhere — `publish = false` means it can't reach crates.io by accident either way, but the LICENSE-MIT/LICENSE-APACHE files a real dual-license claim implies don't exist yet. |
| T-210 | **needs owner confirmation** | A1 | CI's Servo-enabled "latest" release moved from every push to main → weekly (Monday 06:00 UTC) + manual `workflow_dispatch` (commit `9fee225`), per `docs/REBUILD_DIRECTIVE.md` §7.1's Servo-is-expensive mandate. This is a real, visible behavior change to something the pre-rebuild team explicitly built (a dedicated "Job 2: Publish a rolling latest release" pipeline) — not committed to a remote yet (10+ commits sit local, unpushed, per `docs/PROGRESS.md`), so nothing has broken for anyone yet, but flagging before it does. |
| T-211 | done `6cb4c70` (ratified) | A7 | **Opaque origins have no representation.** `ferrite_core::Origin` accepts only `http`/`https`, because every other scheme has an opaque origin that no `OriginScope` could ever admit. `RecordingExecutor` has to record the origin of *every* action, including ones taken on `about:blank`, a `data:` URL or a `blob:` URL. A6 decided the interim model (`dry_run::record::extract_origin`, `docs/handoffs/a06.md`): opaque-scheme URLs are recorded as their literal string rather than a normalized `Origin`; that literal string then fails `Origin::parse` downstream, which is the *correct* effect (unadmittable by any scope, like `js.execute`) but achieved by type mismatch rather than a principled `Origin::Opaque` variant. **A7's resolution: ratified, not replaced.** Having read `Origin`'s doc comment (an opaque origin "is modelling something the scope algebra cannot express") together with ADR-004, A7 concludes `Origin::parse`'s http(s)-only acceptance is the intended design, not a gap — a real `Origin::Opaque` variant would need to be unconditionally unadmittable by every scope to mean anything, which "fails to parse" already achieves with less surface area. `compare` treats a recorded-but-unparseable origin as `out_of_scope_origins` (there's a string to report) and a missing origin (`None`) as `extra_primitives` (nothing to report). Tests: `comparator::tests::an_opaque_scheme_origin_is_out_of_scope_under_its_literal_string`, `comparator::tests::an_event_with_no_recorded_origin_is_an_extra_primitive`. |
| T-216 | done `6cb4c70` | A7 | **`BrowserTool::tool_id()` (`ferrite-agent`) and `ferrite_core::Primitive`'s wire strings disagree for one case.** `BrowserTool::DownloadFile`'s `tool_id()` is `"download.file"`; `Primitive::Download::as_str()` is `"download"`. Found while writing `dry_run::record::ToolEvent::primitive()` (A6), a best-effort bridge from the crate-internal `ToolId` string to `ferrite_core::Primitive` — every other primitive matches exactly, this one doesn't, and `primitive()` returns `None` for it (tested, not silently wrong). **A7's resolution:** a one-entry mapping local to `comparator::resolve_primitive` (`"download.file" -> Primitive::Download`, tried only after `ToolEvent::primitive()` returns `None`), not a change to `BrowserTool::tool_id()` — that string is key material for `dry_run::content::DryRunContent::by_tool_id`'s per-tool reply queues (A6's file, off-limits) and is embedded literally in `ferrite-eval`'s corpus fixtures/tests, so changing it would ripple far outside this charter. Test: `comparator::tests::download_file_attributes_to_web_download_at_an_admitted_origin`. |
| T-212 | open | A11/A7 | **`DomainSuffix` does not reject public suffixes.** A scope authored as `domain_suffix: ["com"]` (or `"co.uk"`) normalizes cleanly and admits the entire TLD — the one way the rebuilt scope algebra can still fail *open*. Needs a public-suffix list or an authoring-time corpus lint. ADR-004's weak-scope stratified reporting does **not** cover it: such a scope reports as `domain_suffix`, not `task_open`, so it looks bounded in the results while being wider than most `task_open` scopes. Found in A2 (commit `2a19b00`) while deciding how far `DomainSuffix::parse` should validate; a PSL is a dependency and an authoring lint is A11's, so neither belonged in A2's scope. |
| T-213 | done `75ba980` | A3 | **Config loading with env overrides, deferred from A2 — landed in `ferrite-model::config`.** `ModelConfig::load()`/`from_env()` reads through an `EnvSource` trait (a `MapEnv` for tests, avoiding `std::env::set_var`'s race across `cargo test`'s parallel threads) rather than touching `std::env` directly. `FERRITE_MODEL_SMALL`/`FERRITE_MODEL_MAIN` have no default per §10.2's no-hardcoded-tag rule; everything else (base URLs, call budget, timeout, response-size ceiling, cache dir) has a documented one. |
| T-215 | in-progress `be891b8` | A6 (done) + A12 (`harness.rs`, remaining) | **`crate::dry_run::RecordingExecutor.strip_enabled` — the literal field D3/T-003 names — is still never set `true` anywhere in production.** A5 closed the sanitizer's own live-excision gap at `tool_decision::prepare_task`. A6 (this session) added `DryRunOrchestrator::set_defense_mode(DefenseMode)`, which derives `detect_enabled`/`strip_enabled` together the same way `tool_decision::prepare_task` does (reusing `DefenseMode::sanitizer_detect_enabled`/`sanitizer_strip_enabled`), so a caller cannot wire detect without strip going forward — but could not touch `ferrite-eval/src/harness.rs`/`ferrite-ui/src/lib.rs` (different crates, not in A6's file list either). **A12's remaining one-line fix, exact location:** `crates/ferrite-eval/src/harness.rs::run_one`, replace `orch.set_detect_enabled(behavior.detect_enabled);` (currently line ~220) with `orch.set_defense_mode(mode);` — `mode: DefenseMode` is already `run_one`'s parameter, and nothing later in that function reads `behavior.detect_enabled` again (only `behavior.loop_runs`). `ferrite-ui/src/lib.rs` needs the equivalent one-line change wherever it constructs/configures its `DryRunOrchestrator`. Depends on / feeds T-004. |
| T-214 | done (workaround identified; not a code fix) | process/infra | **Sharing `CARGO_TARGET_DIR` across git worktrees can serve a stale, deleted worktree's compiled test binary — a real, reproducible false-CI-failure source, not a code bug.** Root cause: `env!("CARGO_MANIFEST_DIR")` (used by `ferrite-core`'s and `ferrite-model`'s fixture-path resolution — see ADR-worthy fix history on `fixtures::fixture_dir()`) gets baked into a crate's compiled *incremental codegen units* at compile time. When a crate is compiled once from inside a git worktree (e.g. an isolated agent's `.claude/worktrees/agent-<id>/`) and the shared `~/.cache/ferrite-target` retains that build's incremental cache, `git worktree remove`-ing the worktree does **not** invalidate those cached codegen units — rustc's incremental system intermittently reuses them for a later, textually-identical compile from the *main* checkout, producing a test binary whose baked-in `CARGO_MANIFEST_DIR` points at a path that no longer exists. Observed directly: `ferrite-core`'s `golden_*` schema-stability tests failed with `missing schema fixture .../.claude/worktrees/agent-<id>/crates/ferrite-core/tests/fixtures/schema/*.json: No such file or directory`, reproducible 100% via `just ci` (check-then-test back to back) after A3's worktree was removed, `cargo clean -p ferrite-core` did **not** fix it (4/4 still failing), a full `rm -rf ~/.cache/ferrite-target` did (10/10 clean after). **Mitigation, not yet automated:** wipe (or at minimum `cargo clean`) the shared target dir after removing any worktree that compiled a crate using `env!("CARGO_MANIFEST_DIR")` for a runtime path, before trusting the next `just test`/`just ci` result. Not filed against any crate — this is a rustc/cargo incremental-compilation interaction with the worktree-per-agent workflow itself, not a defect in `ferrite-core`/`ferrite-model`'s source. Worth a `just` recipe (e.g. `worktree-done` wrapping `git worktree remove` + a target-dir clean) if this workflow continues to be used for future agent charters. |
| T-218 | open | A8 (found), unowned | **The audit chain's tail-truncation defense is a best-effort watermark, not a cryptographic guarantee.** `PersistentAuditLog` (`crates/ferrite-audit-log/src/lib.rs`) now writes a single-row `audit_chain_head` checkpoint (next sequence + last entry hash) transactionally with every `append`, and `load` cross-checks it against the entries actually present — this catches an attacker who deletes tail rows without also fixing the checkpoint (tested: `tests::truncation_attack_dropping_the_tail_is_detected_on_load`). It does **not** catch an attacker with full read/write access to the SQLite file who updates both consistently (tested and documented as a known limit: `tests::truncation_undetected_if_attacker_also_forges_the_checkpoint_head`). Closing this for real needs an external anchor — a separately-stored or periodically-published copy of the latest hash (e.g. signed, or written to a second file/service outside the attacker's assumed access) — which is a genuine design decision belonging to whoever owns the eventual deployment/threat-model story, not a mechanical follow-up. |
| T-219 | open | A8 (found), low priority | **`AuditError::HashMismatch`/`ChainBroken` variants are declared but never constructed anywhere in `ferrite-audit-log`.** Predates this session (present in the file before A8 touched it) — `AuditLog::verify_chain()` returns a bare `bool`, so no code path ever builds these two error variants; they don't trip `dead_code` because they're variants of a `pub` enum, so the lint is silent, but no caller can currently obtain the richer error info the variants suggest exists. Not fixed here: doing so properly means changing `verify_chain()`'s signature from `bool` to `Result<(), AuditError>`, which is a public API change touching 3 external call sites (`ferrite-servo/src/shell.rs`, `ferrite-shell/src/main.rs`, `ferrite-eval/src/harness.rs`, all of which currently use it as a boolean condition/assertion) — real but small work, outside A8's charter of fixing the preimage. |
| T-217 | open | A11/A12/whoever authors real per-task scoping | **`ExpectedFingerprint`'s scope-sourcing policy is explicitly provisional.** `ExpectedFingerprint::from_fingerprint`/`from_legacy_tool_fingerprint` (`crates/ferrite-ipi/src/comparator/expected.rs`) apply one `OriginScope` — derived from a single context URL — uniformly to every capability in a fingerprint, because neither `fingerprint::Fingerprint` (A4) nor the legacy `tool_decision::ToolFingerprint` distinguishes which capability wants which origin (e.g. `scoped.read` narrowed to a mail host vs. `web.read` left open for search results). `ExpectedFingerprint::from_capabilities` already accepts real per-capability precision the moment something authors it — this task is that authoring mechanism: wire `dataset::CaseDefinition`'s existing `scope_rationale` field (or a new per-capability field) into a genuine multi-scope `ExpectedCapabilitySet` for the eval path, and/or a per-task authoring UI for the live agent path. Found by A7 while resolving the D1/T-001 design gap; see `comparator`'s module docs ("Where a per-capability `OriginScope` comes from") for the full reasoning. |
| T-220 | open | A9 (found), unowned | **`ServoEngine::navigate` does not observably complete a real page load in this environment — real Servo networking never appears to start.** After A9 fixed two process-level blockers (the `BrowserEngine: Send` bound, which the real, `Rc`-backed `HeadlessServoSession` cannot satisfy; and Servo's `opts` process-global-singleton panic when more than one `#[test]` fn in a binary constructs a session, since `libtest` spawns a fresh thread per test regardless of `--test-threads`), `ServoEngine::new` succeeds against a real, successfully-built Servo (`docs/BUILD_BUDGET.md`'s A9 entry) — but `navigate()` to a `127.0.0.1` loopback fixture server never completes: a throwaway diagnostic confirmed the fixture server receives no TCP connection attempt at all within the drive-until-loaded window. Leading hypothesis, not confirmed: `HeadlessServoSession` (`ferrite-servo/src/session.rs`) was built to be driven by a genuine winit `EventLoop::run()` tick (as `ferrite-shell` does), and bare repeated `spin_event_loop()` calls with no real OS-level event loop underneath may not be enough to make Servo's networking component (which likely runs on its own thread/process) actually attempt a fetch. Fixing this means either instrumenting/modifying `ferrite-servo::session` (outside A9's file scope) or wrapping a real winit loop inside `ServoEngine` — real, non-trivial follow-up work. See `crates/ferrite-engine-servo/tests/servo_conformance.rs`'s module docs and `docs/handoffs/a09.md`. **Scope check, confirmed live 2026-09-18:** this bug is isolated to the new `ferrite-engine-servo::ServoEngine` wrapper. The live `ferrite-ui`/`ferrite-shell` app, built with `cargo run -p ferrite-shell --features ferrite-servo/servo`, uses the original `ferrite-servo::session`/`shell.rs` integration directly (a real winit event loop, not bare `spin_event_loop()` calls) and was confirmed by the user to render a real page end-to-end. Real browsing in the actual product is not blocked by T-220. |
| T-221 | open | found by A9, feeds A13 | **`ferrite-ipi`'s `Cargo.toml` depends on `ferrite-agent`, which is backwards from the target dependency direction** (`core ← {model, audit, engine} ← ipi ← agent ← {ui, eval, cli}`, `CLAUDE.md`). Confirmed still present (`crates/ferrite-ipi/Cargo.toml`: `ferrite-agent = { workspace = true }`), used for `BrowserTool`/`ToolExecutor`/`AgentToolCall`/`RateLimiter` throughout `ferrite-ipi::dry_run` and `tool_decision`. A9 did not attempt to fix this — reversing it means relocating `BrowserTool`/`AgentRuntime`/`ToolExecutor` (or a replacement) to a crate both `ipi` and `agent` can depend on, which ripples through `ferrite-eval`/`ferrite-ui`/`ferrite-shell` call sites well beyond A9's `ferrite-engine`/`ferrite-engine-servo`/`ferrite-agent` scope. Likely A13 (reconciliation) territory, per the directive's own suggestion. |
| T-222 | open | found by A10, feeds a future `ferrite-ipi` comparator session | **`FingerprintDiff::extra_primitives`/`out_of_scope_origins` record only a tool id or an origin string, never the `(tool, origin)` pair together, for a bucket that structurally has both.** Found while building the consent panel's plain-English per-item summary (A10/T-110): for an out-of-scope-origin item, the panel can only say "these are the origins your task authorized somewhere" (`ExpectedFingerprint::lowered()`'s full scope list), never "primitive P specifically needed origin O", because the diff itself discarded which primitive was involved once it decided the event was out-of-scope. The same gap means `ferrite-ui`'s `FilteredToolExecutor` can only enforce a rejected origin for the two `BrowserTool` variants that carry their own URL (`Navigate`, `DownloadFile`) — a call with no URL of its own (`ReadPage`, `ClickElement`, ...) can't be checked against a rejected origin from the call alone. Fixing this for real means enriching `FingerprintDiff`'s shape in `ferrite-ipi::comparator` (e.g. `Vec<(ToolId, String)>` instead of two separate `HashSet`s) — out of A10's `ferrite-ui`-only file scope; `ferrite-ui` documented the limitation rather than fabricating precision the data doesn't support. |
| T-223 | open | found by A10, unowned | **iced 0.13's `button` widget does not implement `widget::operation::focusable::Focusable`** (verified: `grep -rln Focusable iced_widget-0.13.4/src/` matches only `text_input.rs`/`text_editor.rs`), so a literal keyboard-focus-ring default onto the consent panel's Reject button — asked for by `docs/REBUILD_DIRECTIVE.md` §6/A10 — is not implementable against this pinned iced version's public API. `ferrite-ui` mitigates with the closest available equivalent (Reject listed/styled first per item) plus the property that actually matters for safety: `consent_is_complete` gates `ConsentSubmitted` so there is no path to a silent default-approve regardless of which button a stray keypress reaches. A real fix needs either an iced version bump (re-check `Focusable` support) or a custom focusable button wrapper — deferred, not attempted, given the charter's "small and clearly safe" bar for touching UI framework internals. |
| T-224 | open | found by coordinator (live run), unowned | **The live `ferrite-ui`/`ferrite-shell` app runs entirely on the pre-rebuild agent path — none of A3–A9's new model-provider/engine/loop stack is wired in.** Confirmed by actually running the app: "Run Task" in the agent sidebar calls `ferrite_agent::gemini::GeminiAgent::read_api_key()`, which checks a `gemini_key.txt` file next to the binary or a bare `FERRITE_GEMINI_API_KEY` env var — code that predates `ferrite-model` entirely and has no idea the OS keyring (`ferrite_model::secret::OsKeyring`, service `"ferrite"`) exists, even though a real, working Gemini key is stored there right now. More broadly: `ferrite-ui` never constructs a `ferrite_model::ModelProvider`, never calls `ferrite_engine::BrowserEngine`/`MockEngine`/`ServoEngine`, and never drives `ferrite_agent::browser_loop::run_agent_loop` — it still runs the original `GeminiAgent`/`BrowserTool`/`AgentRuntime`/`ToolExecutor` loop from before this rebuild started (A9 confirmed this path is still load-bearing and deliberately left it alone, which was the right call for A9's own charter, but the live-wiring gap itself was never separately filed until now). Closing this means: constructing a real `ModelProvider` (Ollama/Gemini, config from `ferrite_model::ModelConfig`) in `ferrite-ui`'s startup, replacing `GeminiAgent`'s direct HTTP calls with it, and deciding whether the agent loop itself moves to `browser_loop::run_agent_loop` over a `BrowserEngine` (real UX/behavior change, needs care) or the old loop is kept and only its model-calling internals are swapped (smaller, safer, arguably the right first step). Likely a dedicated charter (A14?) or folded into A12/A13 — not scoped or started as of this entry. |
| T-225 | open | found by A11 (T-203 audit), feeds A12 | **FSR (`docs/REBUILD_DIRECTIVE.md` §13.2) is only computable from `ExecutionRecord` as a case-level proxy, not the literal segment-level ratio the formula defines.** `ExecutionRecord` has no per-segment finding/strip count — only `sanitizer_caught: LayerOutcome` (one value per execution) and, for benign×SanitizerOnly, `final_outcome`'s `BenignFalseFlag`/`BenignNoFlag` split (D4's outcome lattice). A12's metrics module can compute `\|BenignFalseFlag\| / \|benign cases run SanitizerOnly\|` with zero schema changes, but that undercounts/overcounts relative to "segments excised / segments total" whenever a case's content has more than one sentence — `sanitizer/config.rs`'s own unit test only gets the literal per-segment number because each of its 35 fixtures *is* one segment. Fixing this for real means either instrumenting the sanitizer to report a per-case (excised_segment_count, total_segment_count) pair into `ExecutionRecord`, or explicitly documenting the case-level proxy as the reported FSR in `docs/EVALUATION.md` with the gap stated. Decide which in A12. |
| T-226 | open | found by A11 (T-203 audit), feeds A12 | **No field in `ExecutionRecord`/`Timing` carries model token usage, so ΔT (extra model tokens per task, §13.2) cannot be computed from persisted data.** `ferrite_model::response::TokenUsage` (`prompt_eval_count`/`eval_count`, exactly §10.1's wire fields) already exists in `ferrite-model` but nothing threads it into `ferrite_ipi::dataset::Timing` or `ExecutionRecord`. Proposed fix: add `Timing.tokens: Option<ferrite_model::response::TokenUsage>` (or an equivalent field), populated from the fingerprint-predict and agent-loop model calls in `harness.rs`'s orchestration. Not done in A11 because populating it (not just declaring it) requires touching `harness.rs`'s orchestration beyond the "minimal to keep compiling" bound that charter set, and an unpopulated field would itself violate `CLAUDE.md`'s no-dead-code invariant. |
| T-227 | open | found by A11, feeds A12/A13/whoever authors the rest of the corpus | **Corpus-size remainder against `docs/REBUILD_DIRECTIVE.md` §13.3's ~360-case target, stated honestly rather than left implicit.** A11 grew the corpus from 10 to 29 cases: Tier 1 (WebContent) 17 of 120 target, Tier 2 (ToolOutput) 9 of 80 target, Tier 3 (AgentDojo) 3 of 60 target, Benign 7 of 100 target. **0 of the required 10% double-authored cases exist** — every case is `Author::SelfAuthored` or `Author::AgentDojo` (a citation-derived provenance, not a second independent human author); Cohen's κ is therefore not computable and was not fabricated from a fake pair. Closing this needs: (a) a genuinely independent second author (a teammate/professor slice per ADR-008, or a differently-configured session that never saw this session's case designs) to produce the double-authored 10%, and (b) substantially more self-authored volume across all four strata to approach §13.3's targets. A12/A13 should scope their own metrics/power expectations against the actual n=29 (pooled CI half-width will be far wider than §13.3's derivation assumed for n≈360), not the aspirational target. |

## Notes for whoever picks up A1 next

- The workspace was flattened from `Browser/*` to the repo root in A0
  (commit `e474403`), proven content-identical via `cargo metadata`
  before/after. If you find a stale `Browser/`-prefixed path anywhere,
  that's residue, not intentional.
- `docs/ARCHITECTURE.md` does not exist yet. `CLAUDE.md` points to it as
  "not yet written." A1/A2 is the natural place for it to appear, per
  `docs/REBUILD_DIRECTIVE.md` §12's completeness check (deferred until the
  file exists — see `scripts/check_no_archive_links.sh`'s comments).
- `scripts/check_purge.sh` and `scripts/check_no_archive_links.sh` both
  pass clean as of A0's last commit. Wire them into CI as part of T-101 —
  they're currently run-by-hand only.
